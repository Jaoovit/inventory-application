<!DOCTYPE html>
<html>
    <head>
        <title>Search for a coffee</title>
    </head>
    <body>
        <h1>Find a coffee for origin</h1>
        <% for(let i = 0; i < origin.length; i++) { %>
            <p>
                <form action="/origin/<%= origin[i] %>" method="GET">
                    <button type="submit"> <%= origin[i] %> </button>
                </form>
            </p>
        <% } %>
    </body>
</html>