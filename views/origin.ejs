<!DOCTYPE html>
<html>
    <head>
        <title>42 coffee origin</title>
    </head>
    <body>
        <% coffee.map((coffee) => { %>
        <h1><%= coffee.name %></h1>
        <p>Origin: <%= coffee.origin %></p>
        <p>Weigth: <%= coffee.weight %></p>
        <p>Price: €<%= coffee.price %></p>
        <p>We have <%= coffee.quantity %> in our stock</p>
        <% }) %>
        <p>
            <a href="/search">Look for another origin</a>
        </p>
        <p>
            <a href="/">Go back to homepage</a>
        </p>
    </body>
</html>